{
  "name": "go_to_player",
  "description": "Go to the given player.",
  "parameters": {
    "type": "object",
    "properties": {
      "player_name": {
        "type": "string",
        "description": "The name of the player to go to."
      },
      "max_distance": {
        "type": "number",
        "description": "How far the player can be."
      },
      "closeness": {
        "type": "number",
        "description": "How close to get to the player."
      }
    },
    "required": [
      "player_name"
    ]
  }
}
