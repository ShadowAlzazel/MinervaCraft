{
  "name": "follow_player",
  "description": "Actively follows the given player.",
  "parameters": {
    "type": "object",
    "properties": {
      "player_name": {
        "type": "string",
        "description": "The name of the player to follow, usually the user."
      },
      "max_distance": {
        "type": "number",
        "description": "How far the player can be. Defaults to 20."
      },
      "closeness": {
        "type": "number",
        "description": "How close to stay to the player. Defaults to 3."
      }
    },
    "required": [
      "player_name"
    ]
  }
}
